import { StyleSheet } from "react-native";

export const folderCommonStyles = StyleSheet.create({
    row: {
        flexDirection: "row"
    },

    column: {
        flexDirection: "column"
    }
})